import 'package:flutter/cupertino.dart';import 'package:flutter/material.dart';import 'dart:io';class StartPattern extends StatefulWidget {  StartPattern({Key? key, required this.title}) : super(key: key);  final String title;  @override  _StartPattern createState() => _StartPattern();}class _StartPattern extends State<StartPattern> {  final myController = TextEditingController();  late var buttonText = 'Click Me!';  @override  void dispose() {    // Clean up the controller when the widget is disposed.    myController.dispose();    super.dispose();  }  @override  Widget build(BuildContext context) {    //String buttonText = 'Click Me!';    return Scaffold(      appBar: AppBar(        centerTitle: true,        title: Text(widget.title,            style: TextStyle(              color: Colors.orange,              fontSize: 20,            )),      ),      body: Center(        child: Column(          mainAxisAlignment: MainAxisAlignment.center,          children: <Widget>[            Padding(              padding: EdgeInsets.symmetric(horizontal: 16, vertical: 16),              child: TextField(                decoration: const InputDecoration(                    border: OutlineInputBorder(), labelText: 'Enter a number'),                keyboardType: TextInputType.number,                controller: myController,              ),            ),            TextButton(              onPressed: () {                int rows = 6;                for(int i = 0 ; i< rows; i++)                {                  for(int j = 0;j<=i;j++)                  {                    stdout.write(" ");                  }                  for(int k=0;k<=rows-1-i;k++){                    stdout.write("* ");                  }                  stdout.writeln();                }                /*int rows = int.parse(myController.text);                String displayString = "";                for (int i = 0; i < rows; i++) {                  String s = '';                  for (int j = 2 * (rows - i); j >= 0; j--) {                    s = s + '* ';                  }                  displayString = displayString + "\n" + s;                }*/                /*setState(() {                  print(displayString.toString());                  buttonText = displayString.toString();                });*/              },              child: Text(                buttonText, //title                textAlign: TextAlign.center, //aligment              ),            ),          ],        ),      ),      /*floatingActionButtonLocation: FloatingActionButtonLocation.centerFloat,      floatingActionButton: FloatingActionButton.extended(        onPressed: () {          int rows = 6;          String displayString = "";          for(int i = 0 ; i< rows; i++)          {            String s = '';            for(int j = 0; j<=i;j++)            {              s = s + '* ';            }            displayString= displayString+"\n"+s;          }          setState(() {            print(displayString.toString());            buttonText = displayString.toString();          });        },        label: Text(buttonText),        backgroundColor: Colors.orange,      ),*/    );  }}